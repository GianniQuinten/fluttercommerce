# goss.yaml
port:
  tcp:80:
    listening: true
    ip:
      - 0.0.0.0

file:
  /usr/share/nginx/html/index.html:
    exists: true

command:
  nginx -t:
    exit-status: 0
    stdout:
      - "nginx.conf test is successful"
